CREATE TABLE Admin_Employee (
    Emp_ID int NOT NULL AUTO_INCREMENT,
    Email varchar(50) NOT NULL UNIQUE,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255) NOT NULL,
    Password varchar(255) NOT NULL,

     PRIMARY KEY (Emp_ID)
);


INSERT INTO Admin_Employee (Email, LastName, FirstName, Password)
VALUES ();

INSERT INTO Admin_Employee (Email, LastName, FirstName, Password) VALUES ('mihir@gmil.com','patel','mihir','123')



like First name, last name, email , salary, total_leave



CREATE TABLE Employee (
    Emp_ID int NOT NULL AUTO_INCREMENT,
    Email varchar(50) NOT NULL UNIQUE,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255) NOT NULL,
    salary bigint NOT NULL,
    total_leave DECIMAL(5, 2) NOT NULL DEFAULT 0,

     PRIMARY KEY (Emp_ID)
);

CREATE TABLE Employee_Leave (
  Emp_ID int NOT NULL,
   Leave_type DECIMAL(5, 2) NOT NULL,
   Date date NOT NULL,
   Reason varchar(255)

);




 select e.Emp_ID, e.Email from Employee as e join (select Emp_ID, sum(leave_type) from Employee_Leave GROUP BY Emp_ID );




<!-- <form method="post" action="/Register">
        <div>

            <hr>

            <label for="leave_type">Choose a leave type</label>
            <select name="leave_type" id="leave_type">
                <option value=0.5>half day</option>
                <option value=1>full day</option>
            </select> -->

            <!-- <label for="leave_type"><b>leave_type</b></label>
          <input type="number" placeholder="leave type" name="leave_type" id="leave_type" required><br/><br/>
       -->
            <!-- <label for="leave_date"><b>leave date</b></label>
            <input type="date" placeholder="leave date" name="leave_date" id="leave_date" required><br /><br />

            <label for="reason"><b>Reason</b></label>
            <input type="text" placeholder="Enter Reason" name="Reason" id="Reason" required><br /><br />


            <label for="Employee">Choose a Employee</label> 
            <select name="Employee" id="Employee">
                <% if(data.length!=0){%>
                   <% data.forEach(function(data){ %>
                <option value=<%data.Emp_ID%>><%=data.FirstName%></option>
                <%}%>
                <%}%>
             </select>
            <hr>


            <button type="submit">Register</button>
        </div>
    </form> --> 



    select e.Emp_ID,e.salary,e.FirstName,e.LastName, e.Email,l.total_leave from Employee as e join (select Emp_ID, sum(leave_type) as total_leave from Employee_Leave GROUP BY Emp_ID ) as l on e.Emp_ID = l.Emp_ID;


    LastName  FirstName salary